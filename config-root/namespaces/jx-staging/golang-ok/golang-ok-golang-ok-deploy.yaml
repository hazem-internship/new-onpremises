# Source: golang-ok/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: golang-ok-golang-ok
  labels:
    draft: draft-app
    chart: "golang-ok-0.0.6"
    gitops.jenkins-x.io/pipeline: 'namespaces'
  annotations:
    meta.helm.sh/release-name: 'golang-ok'
    wave.pusher.com/update-on-config-change: 'true'
  namespace: jx-staging
spec:
  selector:
    matchLabels:
      app: golang-ok-golang-ok
  replicas: 1
  template:
    metadata:
      labels:
        draft: draft-app
        app: golang-ok-golang-ok
    spec:
      serviceAccountName: golang-ok-golang-ok
      containers:
      - name: golang-ok
        image: "ghcr.io/hazem-internship/golang-ok:0.0.6"
        imagePullPolicy: IfNotPresent
        env:
        - name: VERSION
          value: 0.0.6
        envFrom: null
        ports:
        - name: http
          containerPort: 8080
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 1
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 1
        resources:
          limits:
            cpu: 400m
            memory: 256Mi
          requests:
            cpu: 200m
            memory: 128Mi
      terminationGracePeriodSeconds:
      imagePullSecrets:
      - name: "tekton-container-registry-auth"
